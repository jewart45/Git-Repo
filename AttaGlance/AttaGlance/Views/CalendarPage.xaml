<Page
    x:Class="AttaGlance.CalendarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AttaGlance"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.Resources>
            <!--DataTemplate for Published Date column defined in Grid.Resources.  PublishDate is a property on the ItemsSource of type DateTime -->
            <DataTemplate x:Key="CalendarTemplate" >
                <local:EventBlock/>
            </DataTemplate>

        </Grid.Resources>
        <controls:DataGrid ItemsSource="{Binding PeopleList}" AutoGenerateColumns="True" HorizontalAlignment="Center" VerticalAlignment="Center">

        </controls:DataGrid>
       
    </Grid>
</Page>
