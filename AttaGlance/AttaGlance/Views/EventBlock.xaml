<UserControl
    x:Class="AttaGlance.EventBlock"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AttaGlance"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="75"
    d:DesignWidth="200">
    <Grid Height="75" Width="200">
        <Border BorderBrush="Gray" BorderThickness="2" Margin="-1"/>

        <Grid x:Name="MainGrid" Margin="5,2.5" Background="{Binding BackgroundColor.Value, FallbackValue=Blue}" CornerRadius="15" DoubleTapped="Grid_DoubleTapped">
            <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10">
                <Viewbox  HorizontalAlignment="Left" Margin="0" Width="150" Height="25">
                    <TextBlock Text="{Binding Subject.Value, FallbackValue='Subject Line'}" FontSize="18" Foreground="White" FontWeight="Bold"/>
                </Viewbox>
                
                <TextBlock Text="{Binding Time.Value, FallbackValue='15:00 - 17:30'}" Margin="0,5" HorizontalAlignment="Left" TextWrapping="Wrap" Foreground="White" FontSize="12"/>

            </StackPanel>
        
        </Grid>
    </Grid>
</UserControl>
